# TODO: set to the real deployed backend URL before building for production.
# (No production API URL exists in the repos yet — both apps point at localhost.)
NEXT_PUBLIC_API_URL=https://api.biznavigate.com/api
NEXT_PUBLIC_PUBLIC_BASE_URL=https://biznavigate.com
